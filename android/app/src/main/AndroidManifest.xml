<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ bildirim izni. Bildirimleri Chrome gösteriyor; izin
         isteğini androidbrowserhelper devrediyor. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ paket görünürlüğü: bu sorgu olmadan uygulama telefonda
         TWA destekleyen bir tarayıcı olup olmadığını göremez ve her zaman
         normal sekmeye düşer. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rezzerv">

        <!-- Uygulama tarafındaki bağ beyanı. Sitedeki assetlinks.json ile
             ÇİFT YÖNLÜ eşleşme gerekiyor: site uygulamaya, uygulama siteye
             güvendiğini söylüyor. -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Rezzerv.Launcher">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/launch_url" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/navigationColor" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Sitenin bağlantıları uygulamada açılsın: WhatsApp'tan gelen
                 bir randevu bağlantısı tarayıcı yerine uygulamayı açar. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="@string/site_host"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Açılış ekranı YOK ve bu bilinçli. Kütüphane, verilen drawable'ı
             Bitmap'e çevirmek zorunda; katmanlı bir XML drawable'da genişlik
             -1 hesaplanabiliyor ve uygulama açılışta çöküyor. Açılış görseli
             zorunlu değil, çökme ise ürünün tamamını götürüyor. Görsel
             istenirse Bubblewrap'in yaptığı gibi DÜZ PNG verilmeli.
             Bunu taşıyan FileProvider de kaldırıldı; başka kullanıcısı yoktu. -->

        <!-- Bildirim devri: site Web Push ile bildirim gönderdiğinde
             bildirim Chrome'un değil, bu uygulamanın adıyla görünür. -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.DelegationService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>
</manifest>
